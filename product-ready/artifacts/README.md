# Binary Artifacts

Place optional offline runtime artifacts here:

- `cryon-node-linux-amd64`
- `version.txt`
- `binary-manifest.txt`

These files are ignored by git by default.
For sharing, use GitHub Releases (recommended) or internal artifact storage.
