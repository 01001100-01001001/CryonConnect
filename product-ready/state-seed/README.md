# State Seed Artifacts

Optional seed files for bootstrap:

- `postgres-<db>.dump`
- `redis-dump.rdb`

These files are ignored by git by default.
Do not commit real production data to source control.
